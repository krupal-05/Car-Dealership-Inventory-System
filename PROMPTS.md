# Incubyte Assignment Prompts & TDD Log

This file tracks key prompt instructions, architectural decisions, and TDD steps followed during development.

---

## Mentorship Rules
- Build from absolute scratch.
- Teach step by step.
- Follow strict Test-Driven Development (TDD) (Red-Green-Refactor).
- Explain every concept before writing code.
- Write only one feature at a time.
- Maintain a professional Git history.

---

## Log of Execution Steps

### Step 1: Backend Monorepo & TDD Setup
- Initialized Node project and Jest configuration.
- Built initial health check endpoint `GET /api/health` using TDD.

### Step 2: Multi-Layer Architecture Structure
- Configured layered architecture (`controllers`, `routes`, `middleware`, `models`, `services`, `repository`, `tests`, `config`, `utils`).
- Configured frontend React application layout (`components`, `pages`, `hooks`, `context`, `services`, `layouts`).
